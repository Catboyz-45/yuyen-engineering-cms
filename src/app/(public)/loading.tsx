/**
 * หน้าที่ของไฟล์นี้: หน้าหรือส่วนแสดงสถานะกำลังโหลด เพื่อให้ผู้ใช้ทราบว่าระบบยังทำงานอยู่
 *
 * หมายเหตุสำหรับผู้อ่านที่ไม่เขียนโค้ด: อ่านคำอธิบายนี้ก่อน แล้วไล่ดูชื่อฟังก์ชันและคอมเมนต์ใกล้กฎสำคัญด้านล่าง
 */
import { LoadingView } from "@/components/loading-view";
/** สร้างส่วนหน้าจอ PublicLoading; รับข้อมูลผ่านพารามิเตอร์แล้วคืน React elements สำหรับแสดงผล */
export default function PublicLoading() { return <LoadingView />; }
