"use client";
import { useCallback, useEffect, useState } from "react";
import { Search, ShieldCheck } from "lucide-react";
import { AdminPageHeader } from "@/components/admin-shell";
type Log = { id: string; action: string; targetType?: string; targetId?: string; result: "SUCCESS" | "FAILURE"; createdAt: string; actor?: { displayName: string; username: string } };
export function AuditLog() { const [items, setItems] = useState<Log[]>([]); const [query, setQuery] = useState(""); const [page, setPage] = useState(1); const [pageCount, setPageCount] = useState(1); const [error, setError] = useState(""); const load = useCallback(async () => { const params = new URLSearchParams({ query, page: String(page), pageSize: "30" }); const response = await fetch(`/api/admin/audit?${params}`); const body = await response.json(); if (!response.ok) throw new Error(body.error); setItems(body.items); setPageCount(body.pageCount); }, [page, query]); useEffect(() => { const timer = setTimeout(() => { void load().catch(caught => setError(caught instanceof Error ? caught.message : "โหลดข้อมูลไม่สำเร็จ")); }, 250); return () => clearTimeout(timer); }, [load]); return <><AdminPageHeader title="ประวัติการทำงาน" description="ตรวจสอบเหตุการณ์สำคัญและการเปลี่ยนแปลงข้อมูลย้อนหลัง" action={<span className="tag"><ShieldCheck size={15} /> Audit Log</span>} /><div className="admin-filters"><label className="search-field"><Search size={18} /><input className="field" value={query} onChange={event => { setQuery(event.target.value); setPage(1); }} placeholder="ค้นหากิจกรรม ผู้ใช้ หรือรายการ" /></label></div>{error && <div className="auth-alert warning">{error}</div>}<section className="panel"><div className="table-wrap"><table className="data-table"><thead><tr><th>กิจกรรม</th><th>เป้าหมาย</th><th>ผู้ดำเนินการ</th><th>ผลลัพธ์</th><th>วันและเวลา</th></tr></thead><tbody>{items.map(item => <tr key={item.id}><td>{item.action}</td><td>{item.targetType ?? "–"} {item.targetId ?? ""}</td><td>{item.actor?.displayName ?? "ระบบ"}</td><td><span className={`status ${item.result === "FAILURE" ? "draft" : ""}`}>{item.result === "SUCCESS" ? "สำเร็จ" : "ไม่สำเร็จ"}</span></td><td>{new Intl.DateTimeFormat("th-TH", { dateStyle: "medium", timeStyle: "short" }).format(new Date(item.createdAt))}</td></tr>)}</tbody></table></div><div className="pagination"><button className="btn btn-ghost" disabled={page === 1} onClick={() => setPage(value => value - 1)}>ก่อนหน้า</button><span>หน้า {page}/{pageCount}</span><button className="btn btn-ghost" disabled={page === pageCount} onClick={() => setPage(value => value + 1)}>ถัดไป</button></div></section></>; }
